# Netflix Proxy Server

Simple Express proxy for Netflix auto-login token endpoint.

## Local Development
1. Install dependencies: `npm install`
2. Run server: `npm start`
3. Test: `node test.js`

## Deployed Endpoint
POST `/api/nftoken` with JSON body containing cookies object
